{
  "name": "opencanvas",
  "version": "0.1.0",
  "private": true,
  "description": "A Firefox dark mode and palette extension for Canvas",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/site.js && node --check src/background.js && node --check src/palette.js && node --check src/inline-color.js && node --check src/content.js && node --check src/card-images.js && node --check popup/popup.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
