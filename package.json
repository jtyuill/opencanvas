{
  "name": "canvas-palette",
  "version": "0.1.0",
  "private": true,
  "description": "A Chromium dark mode and palette extension for UTK Canvas",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/palette.js && node --check src/inline-color.js && node --check src/content.js && node --check popup/popup.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
